# Action Atlas Environment Configuration
# IMPORTANT: For monorepo setup, copy this file to BOTH locations:
#   1. Root: cp .env.example .env.local
#   2. Next.js app: cp .env.example apps/web/.env.local
# Then fill in your values in both files

# MongoDB Database Connection
# For Docker local development: mongodb://localhost:27017/actionatlas
# For MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/actionatlas
MONGODB_URI=mongodb://localhost:27017/actionatlas

# OpenAI API Configuration
# Required for generating embeddings and semantic search
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# Redis Configuration (Optional for MVP, recommended for production)
# Used for caching embeddings and search results
# For local development: redis://localhost:6379
# For Redis Cloud: redis://default:<password>@<host>:<port>
REDIS_URL=redis://localhost:6379

# Next.js Configuration
# The base URL for your application
NEXT_PUBLIC_API_URL=http://localhost:3000

# Node Environment
# development | production | test
NODE_ENV=development

# Feature Flags (Optional)
# Enable/disable specific features during development
NEXT_PUBLIC_ENABLE_SEARCH=true
NEXT_PUBLIC_ENABLE_GEOSPATIAL=true

# Logging (Optional)
# trace | debug | info | warn | error
LOG_LEVEL=info

# Rate Limiting (Optional, recommended for production)
# Requests per minute per IP
RATE_LIMIT_RPM=20
